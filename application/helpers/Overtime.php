<?php {
        // $awal = strtotime($in);
        // $akhir = strtotime($out);


        // // rumus menghitung terlambat atau tidak
        // $x =  $awal - $akhir;
        // $jam = floor($x / (60 * 60));
        // $mnt = $x - $jam * (60 * 60);
        // $menit = floor($mnt / 60);
        // $hasil = $jam . ":" . $menit;

        // if ($hasil < 0) {
        //         $ket = "Tepat Waktu";
        //         $menit = 0;
        //         $terlambat = 0;
        // } elseif ($hasil >= 0) {
        //         $ket = "terlambat";
        //         $menit;
        //         $terlambat = $hasil;
        // }

        // // menghitung overtime
        // $xx =  $akhir - $awal;
        // $jamx = floor($xx / (60 * 60));
        // $mntx = $xx - $jamx * (60 * 60);
        $jamx = "tes";
        return $jamx;
}
